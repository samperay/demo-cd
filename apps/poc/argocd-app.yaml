apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: poc-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/samperay/demo-cd.git
    targetRevision: master
    path: apps/poc
  destination:
    server: https://kubernetes.default.svc
    namespace: poc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
